/**
 * Queues can be created with an array,
 */

const queue = [];
queue.unshift(1);
queue.unshift(2);
queue.unshift(3);
queue.pop();
console.log(queue);
queue.pop();
console.log(queue);
queue.pop();
